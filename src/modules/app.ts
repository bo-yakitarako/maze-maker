/* eslint-disable no-param-reassign */

import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const app = createSlice({
  name: 'template',
  initialState,
  reducers: {},
});

export { app };
